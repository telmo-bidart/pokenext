import type { NextPage } from 'next'
import Layout from './Layout'

const Home: NextPage = () => {
  return (
    <Layout title='PokeNext'>
      <h1>{title}</h1>
    </Layout>
  )}

export default Home
